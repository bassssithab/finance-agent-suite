[project]
name = "audit-log"
version = "0.1.0"
description = "Append-only, hash-chained event store for the finance-agent-suite platform chassis"
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=8"]
