[project]
name = "tenancy"
version = "0.1.0"
description = "Prototype multi-tenancy layer (tenants, one-tenant-per-user membership, tenant-scoped data access) for the finance-agent-suite platform chassis — not yet wired into any app or agent"
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=8"]
