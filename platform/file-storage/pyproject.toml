[project]
name = "file-storage"
version = "0.1.0"
description = "Prototype tenant-scoped file storage: store/retrieve bytes gated by a tenancy.TenantScope, with metadata + external-reference links, on local disk"
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=8"]
