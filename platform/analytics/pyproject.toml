[project]
name = "analytics"
version = "0.1.0"
description = "Prototype tenant-scoped usage tracking + aggregation (counts, per-user activity, rate-limit checks) — a queryable usage layer, distinct from the tamper-evident audit-log"
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=8"]
