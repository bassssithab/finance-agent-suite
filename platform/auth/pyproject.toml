[project]
name = "auth"
version = "0.1.0"
description = "Prototype authentication/login layer (username + hashed password + session tokens) for the finance-agent-suite platform chassis — not yet wired into any app or agent"
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=8"]
